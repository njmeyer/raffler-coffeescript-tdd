// Generated by CoffeeScript 1.6.3
describe("Raffler View", function() {
  var invisible_table, names_data;
  names_data = [
    {
      id: 1,
      name: 'Larry',
      winner: false
    }, {
      id: 2,
      name: 'Moe',
      winner: false
    }, {
      id: 3,
      name: 'Curly',
      winner: false
    }
  ];
  invisible_table = document.createElement('table');
  beforeEach(function() {
    this.names_collection = new Raffler.Collections.Entries(names_data);
    return this.names_view = new Raffler.Views.EntriesIndex({
      collection: this.names_collection,
      el: invisible_table
    });
  });
  it("should be defined", function() {
    return expect(Raffler.Views.EntriesIndex).toBeDefined();
  });
  it("should have the right element", function() {
    return expect(this.names_view.el).toEqual(invisible_table);
  });
  it("should have the right collection", function() {
    return expect(this.names_view.collection).toEqual(this.names_collection);
  });
  return it("should render when an element is added to the collection", function() {
    this.names_collection.add({
      name: 'Billy'
    });
    return expect($(invisible_table).children().length).toEqual(4);
  });
});
